{
  "name": "IsoMaestro",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "lint": "echo \"(add a linter later)\"",
    "test": "node --loader ts-node/esm --test --test-reporter=spec tests/unit/pointer.spec.ts tests/e2e/mcp-e2e.spec.ts",
    "mcp": "node dist/server.js",
    "dev": "node --watch-path dist dist/server.js"
  },
  "dependencies": {
    "@types/node": "^20.11.30",
    "pino": "^9.1.0",
    "undici": "^6.19.4",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
